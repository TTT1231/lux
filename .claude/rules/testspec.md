# 测试文件命名规范

测试文件必须使用 `*.spec.ts` 后缀，禁止使用 `*.test.ts`。

| 正确              | 禁止              |
| ----------------- | ----------------- |
| `foo.spec.ts`     | `foo.test.ts`     |

- 适用于 `tests/` 及未来所有测试目录下的测试文件
- `tests/helpers/` 下的工具文件使用普通 `.ts`，不受此规则约束
